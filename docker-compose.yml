services:

  spring-app:
    container_name: cv-whisper-app
    build: .
    restart: unless-stopped
    expose:
      - "8389"
    networks:
      - kokoro-net

  whisper:
    container_name: cv-whisper
    build: ./whisper-service
    restart: unless-stopped
    expose:
      - "5001"
    volumes:
      - C:\Users\Alex\.cache\huggingface:/root/.cache/huggingface
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    deploy:
      resources:
        limits:
          cpus: '10.0'
          memory: 16G
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    networks:
      - kokoro-net

networks:
  kokoro-net:
    external: true
